/* Crear una matriz

Una forma en que podemos crear una matriz es usar un literal de matriz . Un literal de matriz crea una matriz
 envolviendo elementos entre corchetes []. Recuerde del ejercicio anterior, los arreglos pueden almacenar cualquier
  tipo de datos: podemos tener un arreglo que contenga todos los mismos tipos de datos o un arreglo que contenga 
  diferentes tipos de datos. */
['element example', 10, true];

/* La matriz está representada por corchetes []y el contenido dentro.

Cada elemento de contenido dentro de una matriz se denomina elemento .

Hay tres elementos diferentes dentro de la matriz.

Cada elemento dentro de la matriz es un tipo de datos diferente. */

/* También podemos guardar una matriz en una variable. */
let newYearsResolutions = ['Keep a journal', 'Take a falconry class', 'Learn to juggle'];

/* 1 . Declare una variable usando constnamed hobbiesy configúrela igual a una matriz con tres cadenas dentro de ella. */
/* 2 . Úselo console.log()para imprimir hobbiesen la consola. */
const hobbies = ['Ir al gym', 'Jugar a la Ps4', 'Pasear'];
console.log(hobbies); //[ 'Ir al gym', 'Jugar a la Ps4', 'Pasear' ]